<div class="item">
  <div class="content">
    <%= link_to club_leaderboard_path(club), class: 'header' do %>
      <i class="<%= club.country.downcase %> flag"></i> <%= club.name %>
    <% end %>
    <div class="description"><%= club.player_count.to_i %> players</div>
  </div>
</div>
